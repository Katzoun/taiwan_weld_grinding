# Core stack for Point_Cloud_Preprocessing / Weld_Recognition&Grinding_Path_Planning(PointCNN)
# and Eye-to-Hand_Calibration(SVD+ICP). Verified working on Ubuntu 22.04 / Python 3.10.
numpy==2.2.6
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2
scikit-image==0.25.2
opencv-python==5.0.0.93
open3d==0.19.0
matplotlib==3.10.9
tqdm==4.70.0

# Needed only to run the notebooks interactively (not for headless/batch use)
jupyterlab==4.6.3
notebook==7.6.2

# PointCNN inference/training stack. GTX 1060 (compute capability 6.1 / Pascal)
# needs the cu121 build specifically -- the default PyPI torch wheel (cu130) has
# dropped Pascal kernels entirely. torch_geometric must stay <=2.5.x: 2.6+ made
# XConv require pyg-lib>=0.6.0, and no such pyg-lib build exists for torch 2.3.1.
# Install with:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install torch_geometric==2.5.3 pytorch-lightning torchmetrics
#   pip install torch_scatter torch_cluster pyg-lib -f https://data.pyg.org/whl/torch-2.3.1+cu121.html
torch==2.3.1+cu121
torch_geometric==2.5.3
torch_scatter
torch_cluster
pyg-lib==0.4.0+pt23cu121
pytorch-lightning==2.6.5
torchmetrics==1.9.0

# PointCNN_Training.ipynb also imports a module (pointcnn_seg_1024.py) that is
# not present anywhere in this repo. In practice PointCNN_Training_Functions/model/
# pointcnn_seg.py's PointCNNSeg class is architecturally identical (verified by
# loading pointcnn-best_8_2048.ckpt with it -- state_dict matches exactly) and can
# be used in its place for inference. It needed one fix for modern
# pytorch_lightning: pytorch_lightning.utilities.types.EPOCH_OUTPUT was removed
# and is only used there as a type hint.
